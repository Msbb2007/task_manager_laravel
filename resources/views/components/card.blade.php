<div class="stat-card" {{ $attributes }}>
    <h3>{{ $title }}</h3>
    <div class="value">{{ $value }}</div>
    <div class="footer" style="margin-top: 10px; font-size: 0.8rem; color: #bdc3c7;">
        {{ $slot }}
    </div>
</div>

